import {environment} from "../../environments/environment";

export class AppURLs{
  public static backEnd : string = environment.API_URL;
  public static frontEnd : string = environment.Front_OFFICE_URL
}
